# DocEx 协作规则

> 本项目使用 Project Cairn（路标石堆）组织项目知识：`AGENTS.md` 是规则与导航的入口，`cairn/` 是项目知识与状态层。
> 同一目录下的 `CLAUDE.md` 应只包含一行内容：`@AGENTS.md`，以便 Claude Code 读取相同的规则；Codex 会直接读取此文件。

## 项目一句话简介

智能结构化文档数据提取

> 本文件由 `cairn init` 自动生成，已填入本项目的定位与 provider 配置；其他项目在复用此文件前应先运行其自身的初始化。

## 初始化配置

- Graduation provider(s): Obsidian (vault: DevCairn)
- Knowledge base index: docex/INDEX.md
- Graduation target: docex

## 进入项目后的阅读顺序

1. 首先阅读本文件（AGENTS.md）。
2. 如果 `cairn/ROADMAP.md` 存在，阅读其以获取路线图、当前焦点和开放问题（路线图是可选的；精简初始化的项目可能不包含此文件）。
3. 阅读 `cairn/LOG.md` 中最新的记录（最新条目排在最上方），以了解近期进展与关键决策。
4. 根据当前任务需要，阅读相关的 `cairn/` 知识专题文档。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md` (根目录) | 规则与导航 | 极少变更，限制在 60 行以内 |
| `CLAUDE.md` (根目录) | 单行 `@AGENTS.md` 存根 | 编写一次，后续不再触碰 |
| `cairn/ROADMAP.md` | 路线图与进展 | 就地更新，保持简明 |
| `cairn/LOG.md` | 按时间顺序的日志 | 在最上方添加新条目（最新优先），每条限制在 20 行内，仅包含摘要与指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 就地更新；陷阱记录于正文段落中并使用 `contains` 标记；修订需包含指向 LOG 的指针 |
| `cairn/Reference/` | 外部原始资料 | 按需创建；仅允许追加 |
| `cairn/Cited.md` | 知识库引用列表 | 仅包含指针，绝不复制源文档正文 |

> 其他所有文档只有在出现明确信号（需记录决策、解决陷阱、目标跨越单个会话）时才会创建，而不是预先构建空壳。由代码或流程消费的工程资产（如合同/配置/规范）不属于本系统管理；它们保存在代码树中，而非 `cairn/` 中。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则层面的冲突由本文件仲裁解决。
- 业务/设计结论遵循 `cairn/` 知识专题文档中的最新记录，而非旧的 LOG 条目。

## 知识库消费反射

- 在进行具有可复用内核的工作之前（即其产生或依赖的任何结论达到了够格毕业的标准），应先检查 docex/INDEX.md；仅对切实影响了产出结果的文档在 `cairn/Cited.md` 中添加引用（仅记录指针，绝不复制正文内容）。

## 文档协作规则

- 在做出修改前，判断用户期望的是“讨论/建议”还是“直接修改文档”；当用户表示“先看一下/评估一下”时，先提供分析——不要直接重写正式文档。
- 在修正过去的判断时，追加一条修正说明；不要默默进行覆盖重写。
- 不要将未经确认的判断写为既定事实。

## 知识沉淀规则

- 每次取得实质性进展后，在 `cairn/LOG.md` 最上方添加一条记录（摘要 + 指针）；让结论沉淀进 `cairn/` 知识专题文档中。
- **完成答复门槛：** 在声称任何任务完成之前——包括但不限于工作完成或实现、定稿、更新、同步、验证或测试通过；问题得到修复或解决；交付物准备就绪；声称工作已结束，以及语义相同的说法——必须先运行 `references/maintenance.md` 中的 Cairn 检查点；仅更新其触发矩阵所要求的记录并完成验证，然后再回复。明确的只读/禁止修改要求则禁止写入 Cairn。
- 跨项目的可复用经验通过毕业机制沉淀进 docex。
